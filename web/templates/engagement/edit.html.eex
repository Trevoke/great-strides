<h2>Edit engagement</h2>

<%= render "form.html", changeset: @changeset,
                        action: p_engagement_path(@conn, :update, @engagement) %>

<%= link "Back", to: p_engagement_path(@conn, :index) %>
