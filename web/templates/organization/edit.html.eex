<h2>Edit organization</h2>

<%= render "form.html", changeset: @changeset,
                        action: p_organization_path(@conn, :update, @organization) %>

<%= link "Back", to: p_organization_path(@conn, :index) %>
