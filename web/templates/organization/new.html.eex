<h2>New organization</h2>

<%= render "form.html", changeset: @changeset,
                        action: p_organization_path(@conn, :create) %>

<%= link "Back", to: p_organization_path(@conn, :index) %>
